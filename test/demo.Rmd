---
title: "`netrb` Simulation Demos"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)

source('setup.R')
```

## Minnesota Roads

```{r}
kable(fread('tables/demo-mn-attr.csv'))
```

```{r}
m2 <- cbind(1, 1:4)
colnames(m2) <- c('a', 'b')
m2[-1]
m2
```


```{r echo = TRUE}



g <- sample_natural_graph('mn')


x <- metric_vs_del(
 netrb::integrity,
 # diam = 99,
 # unconn_dist = 100,
 graph = g,
 # del_min = 0.1,
 del_max = 0.5,
 nchunks = 5,
 # del_attrs = 'random',
vcount_orig = vcount(g),
 del_attrs = c('random', 'degree', 'pagerank')
)
           
plot_Ys_vs_X(x, y_axis_label = 'Integrity')
```


```{r}
foo <- function(x = c('a', 'b', 'c')) {
  x <- match.arg(x, several.ok = TRUE)
  return(x)
}
foo(c('a', 'b'))
```

