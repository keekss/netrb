---
title: '`netrb` Simulation Demos'
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)

source('setup.R')
```

## Minnesota Roads

```{r}
kable(fread('tables/demo-mn-attr.csv'))
```

```{r}

g <- sample_natural_graph('mn')


x <- metric_vs_del(
 netrb::integrity,
 # diam = 99,
 # unconn_dist = 100,
 graph = g,
 # del_min = 0.1,
 del_max = 0.5,
 nchunks = 5,
 # del_attrs = 'random',
vcount_orig = vcount(g),
 del_attrs = c('random', 'degree', 'pagerank')
)
           
yy <- plot_Ys_vs_X(x, y_axis_label = 'Integrity')
```
